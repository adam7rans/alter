# Personal affirmations — each user creates their own from the example.
affirmations.txt

# Personal launchd plists — generated by install.sh from the .template files.
com.adam.*.plist
com.*.local.plist

# Compiled binaries — users build from source via install.sh.
/breathe
/affirm
/lookaway
/alterprefs

# macOS junk
.DS_Store

# Build / log artifacts
*.log
*.dSYM/
